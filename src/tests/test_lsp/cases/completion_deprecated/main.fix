module Main;

namespace Hoge {
    new_func : I64 -> I64;
    new_func = |x| x + 1;

    old_func : I64 -> I64;
    old_func = |x| x + 1;
    DEPRECATED[old_func, "Use `new_func` instead."];
}

main : IO ();
main = (
    let _ = new_func;
    pure()
);
