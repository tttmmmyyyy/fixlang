module Main;

// Applying an alias that declares one type variable twice to two different types ends the
// analysis in a panic.
type Alias a a = a;

type UsesAlias = unbox struct { x : Alias I64 Bool };

main : IO ();
main = println("x");
