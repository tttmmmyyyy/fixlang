[general]
name = "threading-lib"
version = "0.1.0"
fix_version = "*"

[build]
files = ["lib.fix"]

# A library that needs multi-threading asks for it here. The project being built decides the
# setting, so this leaves it off for anything that depends on this library.
threaded = true
