module Main;

import Std::{Monad::pure, IO, Path};

a : Path;
a = "hoge.fix";

trait a : ToPath {
    to_path : a -> Path;
}

impl Path : ToPath {
    to_path : Path -> Path = |p| p;
}

main: IO ();
main = (
    pure()
);