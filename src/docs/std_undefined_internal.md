This function is the internal implementation of `Std::undefined`, taking a null-terminated byte array as a message argument.

# Parameters

* `msg` - The message to print to the stderr when the undefined value is reached.