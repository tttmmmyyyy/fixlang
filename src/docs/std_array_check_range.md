Given an array and an index, checks whether the index is within the bounds of the array.

If not, this function aborts the program.

# Parameters
* `idx` - The index to check.
* `arr` - The array to check the index against.