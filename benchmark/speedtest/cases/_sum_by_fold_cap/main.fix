module Main;

sum_by_fold_cap : Array I64 -> I64;
sum_by_fold_cap = |arr| (
    range(0, arr.get_size).fold(0, |i, sum| sum + arr.@(i))
);

main : IO ();
main = (
    let arr = Array::from_map(100000, |i| i);
    assert_eq(|_|"sum_by_fold_cap", sum_by_fold_cap(arr), 4999950000);;
    pure()
);