module Main;

// Although there are overlapping implementations of `Show` of `I64`, we only import one of them.
import ImplTraitA;

main : IO ();
main = (
    eval assert_eq(|_|"", 42.show, "(A) 42");
    pure()
);