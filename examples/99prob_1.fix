/*
https://wiki.haskell.org/99_questions/1_to_10

Problem 1
(*) Find the last element of a list.
*/

module Main;

main : IOState -> ((), IOState);
main = |io| (
    let iter = Iterator.from_array([1,2,3,4]);
    let last = iter.take_last.unwrap;
    io.println!(last.to_string)
);