# P28 (参照の持ち手はちょうど 1 つ) の証明

対象コミットは `35d24c30fbd7828359a101a8779ed7571fff298b` である。

この文書が立つのは README の定義 D2、D3、D4、D7、D8、D9、D10、D11、D12、D14、D16、D22、D23、D24、
D25、D26 と仮定 A5、A8、A17 の上である。証明は 1 本の構造化証明で、その QED が P28 である。

P28 は依存の順の先頭 (層 0) にあるので、この文書は命題を 1 つも引用しない。引用するのは定義と仮定と
コードだけである。

## 0. この文書が README の外から読む 4 つ

**この 4 つは README に無い。**この文書はそれを補って読み、証明はその読みの上に立つ。README を書き換える
のはこの文書の仕事ではないので、4 つとも本文で名前を付けて呼び、どの段がどれを読むかを書く。第 4 節が、
それぞれについて README が現に何と書いているかと、要る形を述べる。

**(H-L) 生きているオブジェクト。**
D25 は「生きているオブジェクト」を持ち手の 1 つに挙げるが、この語を定義していない。この文書は
**割り当てられて、まだ解放されていない (D7、D24 の (F)) オブジェクト**の意味に取る。

**(H-D12) 入力が RC 規律を満たす。**
`P` のすべての本体が D11 を満たす (すなわち `P` が D12 を満たす)。README の P28 の言明はこの仮定を
持たない。第 4.1 節が、これが無いと P28 が偽になることを示す。読む段は 2 つで、(S-a) を `<2>2` の
`Release` と消費の場合が、(S-b) を `<2>5` ((E4) の段) が読む。(S-c) は読まない。

**(H-A) オペランドを適用する `Llvm` の段の行き先。**
D24 の (E2) の行き先の欄を、次のものに読み替える。

> その op が書き込む先のオブジェクト (この段が割り当てるものでも、既に在るものでもよい) の leaf、環境、
> **この段が作る活性化の `Obl`**、あるいは処分。

**(H-F) `Destructor` の解放が作る活性化の初期 `Obl`。**
D24 の (F) が `Destructor` について述べる活性化 `b` について、次を読む。

1. `o` の `_value` の leaf が持つ参照が `o` を離れ、`Obl(b)` に入る。
2. `o` の `_dtor` の leaf が指すオブジェクトへの参照が 1 つ新しく作られ (`H` が 1 上がる)、`Obl(b)` に
   入る。
3. `b` が返す `IO` の動作の活性化が返す参照が、`o` の `_value` の leaf に入る。
4. (F) の「`o` が持つ参照をすべて処分し」が処分するのは、**この 3 つの後に** `o` が持つ参照、すなわち
   `_value` に入った 3 の結果と、もとの `_dtor` である。

## 1. 記法

プログラム `P` の 1 つの実行 `X` を固定する。`X` は段の列 `s_1, s_2, ...` である (D24)。

- **時点** — `t_0` を `s_1` の前、`t_i` を `s_i` の後 (`i ≥ 1`) の時点とする。段は不可分なので、段の
  内部に時点は無い (D24)。(F) の解放と、(F) が作る活性化の内側にも時点は無い (D24)。
- **`Liv(t)`** — 時点 `t` に生きている活性化 (D23) の集合。
- **`Obj(t)`** — 時点 `t` に生きているオブジェクト ((H-L)) の集合。
- **`Cnt(t)`** — 時点 `t` に計数下 (D26) のオブジェクトの集合。
- 計数下オブジェクト `O` について:
  - `H_t(O)` — `O` の参照カウント (D7)。
  - `A_t(O) := Σ_{a ∈ Liv(t)} (Obl(a) が持つ O への参照の個数)`。
  - `B_t(O) :=` 対 `(o', λ)` の個数。ここで `o' ∈ Obj(t)`、`λ` は `o'` が保持する値の boxed leaf (D4)
    であって、`t` において inhabited (D16) であり、null ポインタでなく、`obj(o', λ) = O` である。
  - `C_t(O) :=` `E` が持つ `O` への参照の個数 (D22)。
- **`INV(t)`** — すべての `O ∈ Cnt(t)` について `H_t(O) = A_t(O) + B_t(O) + C_t(O)`。

`A`、`B`、`C` は D25 の 3 種の持ち手に 1 対 1 に対応する。`B` が対を数えるのは A5 による -- 生きている
オブジェクトの inhabited で非 null で計数下を指す leaf が、参照をちょうど 1 つ持つ。

## 2. 証明

**P28.** ASSUME (H-D12): `P` のすべての本体が D11 を満たす
        PROVE  `X` のすべての時点 `t` について、処分されていない各参照は D25 の 3 種の持ち手の
               ちょうど 1 つに属する

`<1>1.` P28 の結論は「`X` のすべての時点 `t` について `INV(t)`」と同値である。

  `<2>1.` `H_t(O)` は `O` への未処分の参照の総数である。
    BY D8
  `<2>2.` `A_t(O) + B_t(O) + C_t(O)` は、D25 の 3 種の持ち手が `O` について持つ参照の個数の総和である。
    BY D25, A5 DEF `A_t`, `B_t`, `C_t`
  `<2>3.` 同じオブジェクトへの参照は互いに区別されない。
    BY D8
  `<2>4.` 3 個の非負整数の和がある非負整数に等しいことと、その個数の族が対応する多重集合の分割を与える
    ことは同値である。
    BY `<2>3` (参照が区別されないので、オブジェクトごとの個数を決めれば分割が 1 つ決まる)
  `<2>5.` QED
    BY `<2>1`, `<2>2`, `<2>4`

`<1>2.` `X` の時点は `t_0, t_1, ...` で尽き、段の内部に時点は無い。
  BY D24 (実行は段の列であり、段は不可分である。(F) の解放も、(F) が作る活性化も、その内側に D24 の
  時点を持たない) DEF 時点

`<1>3.` `INV(t_0)`。

  `<2>1.` 時点 `t_0` では、`X` のどの段も実行されていない。
    BY `<1>2` DEF 時点
  `<2>2.` 参照は D10 の生成によって作られ、D10 の生成はすべて段の中で起きる。
    BY D8 (「参照は D10 の**生成**によって作られ」), D24 (実行は段の列であり、段の外に何も起きない。
    生成を挙げるのは (E2) の生成の表と (E4) の受け渡しである)
  `<2>3.` `t_0` に未処分の参照は無く、すべてのオブジェクト `O` について `H_{t_0}(O) = 0` である。
    BY `<2>1`, `<2>2`, D8 (`H(O)` は未処分の参照の総数)
  `<2>4.` `A_{t_0}(O) = 0`。
    `<3>1.` `Liv(t_0) = ∅` である。活性化を作るのは (E1)、(E3)、(E7)、オペランドを適用する `Llvm` の
      (E2) の段、および `Destructor` の解放 (F) であり、いずれも段の中で起きる。
      BY `<2>1`, D24 (「活性化の林」の段落と (F))
    `<3>2.` QED
      BY `<3>1` DEF `A_t`
  `<2>5.` `B_{t_0}(O) = 0`。
    `<3>1.` `B_t` が数える対 `(o', λ)` の各々について、`o'` はその leaf に参照を 1 つ持つ。
      BY A5, D25
    `<3>2.` QED
      BY `<3>1`, `<2>3` (参照が 1 つも無いので、そのような対は無い)
  `<2>6.` `C_{t_0}(O) = 0`。
    BY `<2>3`, D22 (`E` は参照の多重集合である)
  `<2>7.` QED
    BY `<2>3`, `<2>4`, `<2>5`, `<2>6` DEF `INV`

`<1>4.` 次の 6 種を**素動作**と呼ぶ。素動作は `H = A + B + C` を保つ。

  DEFINE 素動作 ==
  - **(α) 受け渡し**: 1 つの参照が 1 つの持ち手を離れ、別の 1 つの持ち手に入る。`H` は変わらない。
  - **(β) 生成**: 既にあるオブジェクト `O` への参照が 1 つ新しく作られ、1 つの持ち手に入る。`H(O)` が
    1 上がる。
  - **(γ) 割り当て**: 新しいオブジェクト `O` が `Obj` に入る。その値は、inhabited で非 null で計数下を
    指す boxed leaf を 1 つも持たない。`O` への参照が 1 つでき (`H(O) = 1`)、1 つの持ち手に入る。
  - **(δ) 処分**: 1 つの持ち手が持つ `O` への参照が 1 つ無くなる。`H(O)` が 1 下がる。
  - **(ε) 解放**: `H(o) = 0` である計数下のオブジェクト `o` について、`o` が持つ参照 (D25) を (δ) で
    すべて処分し、`o` を `Obj` から取り除く。
  - **(ζ) グローバル化**: 計数下のオブジェクト `O` が `Cnt` を出て、グローバル状態になる (D26)。

  `<2>1.` (α) は `H = A + B + C` を保つ。
    左辺は変わらず、右辺は 1 つの持ち手の個数が 1 減り、別の 1 つの持ち手の個数が 1 増える。
    BY DEF (α), `A_t`, `B_t`, `C_t`
  `<2>2.` (β) は保つ。左辺が 1 増え、右辺も 1 つの持ち手の個数が 1 増える。
    BY DEF (β)
  `<2>3.` (γ) は保つ。
    `<3>1.` `O` について: 左辺は `H(O) = 1`、右辺は入った持ち手の 1 だけである。`O` は (γ) の前は
      `Obj` にも `Cnt` にも無いので、`INV` は `O` について何も課していない。
      BY DEF (γ), `INV`
    `<3>2.` `O` 以外の計数下オブジェクト `O'` について: `O` の値は計数下を指す inhabited で非 null な
      leaf を持たないので `B(O')` は変わらず、`H(O')`、`A(O')`、`C(O')` も変わらない。
      BY DEF (γ), `B_t`
    `<3>3.` QED
      BY `<3>1`, `<3>2`
  `<2>4.` (δ) は保つ。左辺が 1 減り、右辺も 1 つの持ち手の個数が 1 減る。
    BY DEF (δ)
  `<2>5.` (ε) は保つ。
    `<3>1.` (ε) の中の各処分は (δ) であり、`<2>4` によって `H = A + B + C` を保つ。
      BY DEF (ε), `<2>4`
    `<3>2.` それらの処分の後、`o` が持つ参照は無い。よって `o` を `Obj` から取り除いても `B` は
      変わらない。
      BY DEF (ε), `B_t` (`B_t` が `o` について数える対は、`o` が持つ参照と 1 対 1 である -- A5、D25)
    `<3>3.` `o` 自身について: `H(o) = 0` であり、`<3>1` の直前の状態で `H(o) = A(o) + B(o) + C(o)` が
      成り立つので `A(o) = B(o) = C(o) = 0` である。よって `o` を `Obj` から取り除いても
      `A(o) + B(o) + C(o)` は 0 のままである。
      BY DEF (ε), `A_t`, `B_t`, `C_t`
    `<3>4.` QED
      BY `<3>1`, `<3>2`, `<3>3`
  `<2>6.` (ζ) は保つ。
    `<3>1.` `O` は `Cnt` を出るので、`INV` は `O` について何も課さなくなる。
      BY DEF `INV`, (ζ)
    `<3>2.` `O` 以外の計数下オブジェクト `O'` について、`H(O')`、`A(O')`、`C(O')` は変わらない。
      (ζ) は参照を作らず、処分せず、渡さない。
      BY DEF (ζ)
    `<3>3.` `B(O')` も変わらない。`B_t` が数える対 `(o', λ)` の条件は `o' ∈ Obj(t)` と `λ` が
      inhabited・非 null であることと `obj(o', λ) = O'` であり、そのどれも `o'` が計数下かグローバル
      状態かを問わない。
      BY DEF `B_t`, (ζ)
    `<3>4.` QED
      BY `<3>1`, `<3>2`, `<3>3`
  `<2>7.` QED
    BY `<2>1`, `<2>2`, `<2>3`, `<2>4`, `<2>5`, `<2>6`

`<1>5.` `X` の各段は素動作の有限列である。

  `<2>1.` CASE (E1) 環境が活性化を作る段。
    `<3>1.` この段は、作られる活性化 `a` の初期 `Obl` (D10 の初期値) の各参照を `E` から渡す。
      BY D24 の (E1)
    `<3>2.` 渡した後、環境はそれを持たない。
      BY A17 (i)
    `<3>3.` よってこの段は、`a` の初期 `Obl` の各参照についての (α) (`E` から `Obl(a)` へ) の有限列で
      ある。`a` は段の後に生きている活性化になり、その `Obl` は D10 の初期値である。
      BY `<3>1`, `<3>2`, D23 (活性化が始まった時点での `Obl` は D10 の初期値である)
    `<3>4.` QED
      BY `<3>3`

  `<2>2.` CASE (E2) 節点の段のうち、活性化を作らないもの。
    節点の種類は D2 の 6 種であり、`Let` の右辺は D2 の 5 種である。順に見る。

    `<3>1.` CASE `Let(x, Var(y), k)`。素動作は無い。
      D9 の移動の行であり、D10 より移動は `Obl` を変えない。`x` と `y` はどちらも同じ活性化 `a` の
      スロットなので、D25 の持ち手は `a` のままである。
      BY D9 (移動の表の `Let(x, Var(y), k)` の行), D10 (移動は `Obl` を変えない), D25
    `<3>2.` CASE `Retain(v, π, s, k)`。`π` の下の inhabited な各 leaf `λ` について、`obj(v, λ)` が
      計数下ならば (β) 1 つ (持ち手は `Obl(a)`)、グローバル状態ならば素動作は無い。
      BY D10 (`Retain` の行), D24 の (E2) の `H` の表 (`Retain` は +1), D26 (グローバル状態の
      オブジェクトを指す leaf は参照を持たない), A8 (グローバル状態への `Retain` は `H` を変えない)
    `<3>3.` CASE `Release(v, π, s, k)`。`π` の下の inhabited な各 leaf `λ` について、`obj(v, λ)` が
      計数下ならば (δ) 1 つ、グローバル状態ならば素動作は無い。
      `<4>1.` D10 は `π` の下の inhabited な各 leaf につき `Obl(a)` から参照を 1 つ取り除き、`H` を
        1 下げる。D24 の (E2) の行き先の表は、この参照の行き先を処分とする。
        BY D10 (`Release` の行), D24 の (E2) の行き先の表 (`Release` の行)
      `<4>2.` 取り除かれる参照はその時点の `Obl(a)` に入っている。
        BY (H-D12) の (S-a) (D11)
      `<4>3.` グローバル状態のオブジェクトについては、`H` は変わらず、そのオブジェクトを指す leaf は
        参照を持たない。
        BY A8, D26
      `<4>4.` QED
        BY `<4>1`, `<4>2`, `<4>3` DEF (δ)
    `<3>4.` CASE `Destructure(c, fs, s, k)` で `c` が boxed。
      各名前付きフィールドの各 inhabited な leaf につき (β) 1 つ (持ち手は `Obl(a)`) と、`c` の参照
      1 つにつき (δ) 1 つ。
      BY D10 (生成の表の「boxed 容器の `Destructure` の各名前付きフィールドの各 leaf」の行と、消費の
      「捨てる」の行), D24 の (E2) の `H` の表と行き先の表 (boxed 容器の `Destructure` の行),
      (H-D12) の (S-a) (取り除かれる `c` の参照が `Obl(a)` に在ること)
    `<3>5.` CASE `Destructure(c, fs, s, k)` で `c` が unbox。
      名前が付いていないフィールドの inhabited な各 leaf につき (δ) 1 つ。名前付きフィールドは D9 の
      移動なので素動作は無い (`<3>1` と同じ理由で持ち手は `a` のままである)。
      BY D9 (消費の表の unbox 容器の行と移動の表の名前付きフィールドの行), D10, D24 の (E2) の行き先の
      表 (unbox 容器の `Destructure` の行), (H-D12) の (S-a)
    `<3>6.` CASE `Eval(v, k)`。素動作は無い。
      BY D9 (「`Eval(v, k)` と `Let(x, Match(v, arms), k)` の `Match` 節点自身は、参照を作らず、移さず、
      手放さない」)
    `<3>7.` CASE `Ret(x)` がアーム本体の `Ret` である場合。素動作は無い。D9 の移動の行であり、`x` も
      `Match` の束縛変数も同じ活性化 `a` のスロットである。
      BY D9 (移動の表の「`Match` のアーム本体の `Ret(x)`」の行), D10, D25
    `<3>8.` CASE `Ret(x)` が関数本体の終端の `Ret` である場合。この段は (E2) ではなく (E4) である。
      BY D24 ((E2) の「D9 の消費の表の残る 2 行 -- `App(callee, args)` と関数本体の終端の `Ret(x)` --
      は、それぞれ (E3) と (E4) が述べる」)
    `<3>9.` CASE `Let(x, Match(v, arms), k)`。`Match` 節点自身には素動作が無い。選ばれたアームが
      boxed union の変位アームであるとき、payload の inhabited な各 leaf につき (β) 1 つ (持ち手は
      `Obl(a)`)。unbox union の変位アームと catch-all アームの payload 束縛は D9 の移動なので素動作は
      無い。
      BY D9 (「`Match` 節点自身は、参照を作らず、移さず、手放さない」、移動の表の unbox union の行と
      catch-all の行), D10 (生成の表の「boxed union の変位アームの payload の各 leaf」の行),
      D24 の (E2) の `H` の表 (同じ行は +1), D21 (1 つの活性化では 1 つのアームが選ばれる)
    `<3>10.` CASE `Let(x, Closure(f, caps), k)` で `caps` が空。素動作は無い。capture ポインタは
      null であり、そこにオブジェクトも参照も無い。
      BY D24 の (E2) の `H` の表 (`Closure` の行の但し書き), A5 (null の leaf の例外)
      CODE `src/rc_ir/codegen.rs: Generator::build_rc_closure`
    `<3>11.` CASE `Let(x, Closure(f, caps), k)` で `caps` が空でない。capture object の (γ) 1 つと、
      各 capture の inhabited な全 boxed leaf につき (α) 1 つ (`Obl(a)` から capture object の leaf へ)。
      `<4>1.` この段は capture object を新しく割り当て、`H` は 1 である。
        BY D24 の (E2) の `H` の表 (`Closure` の行), D10 (生成の表の `Closure` の行)
      `<4>2.` 割り当ての直後、capture object の欄はまだ埋まっていない。埋めるのは各 capture の値の
        書き込みであり、それが (α) である。
        CODE `src/rc_ir/codegen.rs: Generator::build_rc_closure` (`create_obj` で capture object を
        作り、その後 `captures` を `insert_field_as` で 1 つずつ書き込む)
        CODE `src/object.rs: create_obj` (「A boxed type is allocated on the heap ... with its
        remaining fields left undefined for the caller to fill in」)
      `<4>3.` 各 capture の inhabited な全 boxed leaf の参照が `Obl(a)` を離れ、行き先はこの段が
        割り当てる capture object である。`H` は変わらない。
        BY D9 (消費の表の `Closure` の行), D24 の (E2) の行き先の表 (`Closure` の行と「行き先のある
        参照は存在し続け、`H` は変わらない」), (H-D12) の (S-a)
      `<4>4.` QED
        BY `<4>1`, `<4>2`, `<4>3` DEF (γ), (α)
    `<3>12.` CASE `Let(x, App(callee, args), k)`。この段は (E2) ではなく (E3) であり、その後の再開は
      (E4) である。
      BY D24 ((E2) の「D9 の消費の表の残る 2 行 ... は、それぞれ (E3) と (E4) が述べる」)
    `<3>13.` CASE `Let(x, Llvm(gen, args), k)` で、その op が活性化を作らず、プロセスも終えない。
      `<4>1.` `Obl(a)` を離れる参照は D9 の `Llvm` の行が定めるものであり、その各々の行き先は、
        この op が書き込む先のオブジェクトの leaf、環境、または処分である。前 2 つは (α)、最後は (δ)
        である。
        BY D9 (消費の表の `Llvm` の行), D24 の (E2) の行き先の表 (`Llvm` の行) と (H-A),
        (H-D12) の (S-a)
      `<4>2.` 作られる参照は D10 の生成の表の `Llvm` の行が定めるものであり、A3 の宣言によって、単一の
        `Fresh` なら (γ)、単一の `Unknown` なら (β)、複数の元なら実行路ごとに (γ) か (β) である。空集合
        と宣言された leaf は inhabited にならないので素動作は無い。持ち手はいずれも `Obl(a)` である。
        BY D10 (生成の表の `Llvm` の行とその直後の段落), D24 の (E2) の `H` の表 (`Llvm` の行), A3
      `<4>3.` 単一の `Arg(j, σ)` と宣言された結果の leaf には素動作が無い。D9 の移動の行であり、
        オペランドと結果はどちらも `a` のスロットである。
        BY D9 (移動の表の「`Llvm` の素通し leaf」の行), D10 (移動は `Obl` を変えない), A3 (単一の
        `Arg(j, σ)` は「第 `j` オペランドの leaf `σ` と**同じ参照**」), D25
      `<4>4.` QED
        BY `<4>1`, `<4>2`, `<4>3`
    `<3>14.` QED
      場合は尽きている。D2 は節点を 6 種 (`Let`、`Retain`、`Release`、`Destructure`、`Eval`、`Ret`)、
      `Let` の右辺を 5 種 (`Var`、`App`、`Closure`、`Llvm`、`Match`) と定める。`Retain` は `<3>2`、
      `Release` は `<3>3`、`Destructure` は `<3>4` と `<3>5` (容器が boxed か unbox かで尽きる)、
      `Eval` は `<3>6`、`Ret` は `<3>7` と `<3>8` (アーム本体の `Ret` か関数本体の終端の `Ret` かで
      尽きる -- D3)、`Let` は `<3>1`、`<3>9`、`<3>10`、`<3>11`、`<3>12`、`<3>13` である
      (`Closure` は `caps` が空かどうかで尽きる)。
      BY D2, D3, `<3>1` - `<3>13`

  `<2>3.` CASE (E2) 節点の段のうち、オペランドを適用する `Llvm` のもの。
    `<3>1.` この段は活性化 `b` を作り、`a` は `b` が終わるまで中断中である。
      BY D24 の (E2) の「`Let(x, Llvm(gen, args), k)` の段は、活性化を作りうる」の段落
    `<3>2.` `Obl(a)` を離れる参照は D9 の `Llvm` の行が定めるものであり、その各々の行き先は、この op が
      書き込む先のオブジェクトの leaf、環境、`Obl(b)`、または処分である。前 3 つは (α)、最後は (δ) で
      ある。
      BY D9 (消費の表の `Llvm` の行), (H-A), (H-D12) の (S-a)
    `<3>3.` 作られる参照と、単一の `Arg(j, σ)` と宣言された結果の leaf については `<2>2` の `<3>13` の
      `<4>2`・`<4>3` と同じである。ただし D24 は、`b` から受け取る leaf についてこの表を読まない。
      BY D10, D24 の (E2) の `H` の表と (E4) の「このとき (E2) の生成の表の `Llvm` の行はその leaf に
      ついて読まない」, A3
    `<3>4.` `b` は段の後に生きている活性化になり、その `Obl` は `<3>2` が `Obl(b)` へ渡した参照である。
      BY `<3>1`, `<3>2`, D23
    `<3>5.` QED
      BY `<3>2`, `<3>3`, `<3>4`

  `<2>4.` CASE (E3) 呼び出しの段。
    `<3>1.` D9 の `App` の行が消費する各参照が `Obl(a)` を離れ、呼び出し先の本体の新しい活性化 `b` が
      作られて、それらの参照が `Obl(b)` の初期値になる。
      BY D24 の (E3)
    `<3>2.` 取り除かれる参照はその時点の `Obl(a)` に入っている。
      BY (H-D12) の (S-a)
    `<3>3.` `H` は変わらない。
      BY D9 (「渡す先のある消費 (`App` の引数、`Closure` の capture、終端の `Ret`) では `H` は変わらず、
      参照は渡された先が持つ」), D24 の (E2) の `H` の表 (`App` の行は「**変わらない**」)
    `<3>4.` `a` は中断中の間も生きている活性化であり、その残りの `Obl` は `A` に数えられ続ける。
      BY D23 (生きている活性化とは、始まって終わっていない活性化である)
    `<3>5.` QED
      この段は `<3>1` が挙げる各参照についての (α) (`Obl(a)` から `Obl(b)` へ) の有限列である。
      BY `<3>1`, `<3>2`, `<3>3`, `<3>4`

  `<2>5.` CASE (E4) 返りの段。
    `<3>1.` `b` の終端の `Ret(x)` が消費する参照 -- `x` の inhabited な全 boxed leaf の参照 -- が
      `Obl(b)` を離れ、`b` を作った側へ入る。行き先は 3 つある。(E3) が作ったならば呼び出し元 `a` の
      `Obl(a)`、環境 (E1) が作ったならば `E`、オペランドを適用する `Llvm` の (E2) の段が作ったならば
      その段を実行している活性化の `Obl` である。
      BY D24 の (E4)
    `<3>2.` 取り除かれる参照はその時点の `Obl(b)` に入っている。
      BY (H-D12) の (S-a)
    `<3>3.` `H` は変わらない。
      BY D9 (「渡す先のある消費 (... 終端の `Ret`) では `H` は変わらず、参照は渡された先が持つ」)
    `<3>4.` この消費の後、`Obl(b)` は空である。
      BY (H-D12) の (S-b) (D11: 「実行路の終端の `Ret(v)` において、その `Ret` の消費を行った後の
      `Obl` は空である」)
    `<3>5.` `b` はこの段で終わり、以後 `Liv` に入らない。`<3>4` より、`b` が `Liv` を出ることで `A` が
      失うものは無い。
      BY D23 (活性化が終わるとは、終端の `Ret` に着き、その `Ret` の消費を行うことである), `<3>4`
      DEF `A_t`
    `<3>6.` QED
      この段は `<3>1` が挙げる各参照についての (α) の有限列であり、`<3>5` がそれ以外の変化の無いことを
      与える。
      BY `<3>1`, `<3>2`, `<3>3`, `<3>5`

  `<2>6.` CASE (E5) グローバル化の段。
    `<3>1.` この段は、グローバル初期化子の値が到達するオブジェクトのグラフ全体に印を付ける。印の付いた
      オブジェクトはこの段からグローバル状態になる。
      BY D24 の (E5)
    `<3>2.` この段は参照を作らず、渡さず、処分しない。`mark_global` はオブジェクトを読んで印を付ける
      だけである。
      BY D32 の (読み-2) (`mark_global` の走査は起点のオブジェクトと、そこから到達できるオブジェクトを
      読む)
      CODE `src/generator.rs: Generator::mark_global` (`build_traverser_work` を
      `TraverserWorkType::mark_global()` で呼ぶ)
    `<3>3.` QED
      この段は `<3>1` が挙げる各オブジェクトについての (ζ) の有限列である。
      BY `<3>1`, `<3>2` DEF (ζ)

  `<2>7.` CASE (E6) 中断の段。素動作は無い。
    D24 の (E6) は、この段が参照を作ること、渡すこと、処分することのどれも述べない。この段の後に段は
    無い。
    BY D24 の (E6)

  `<2>8.` CASE (E7) グローバルの初期化の段。素動作は無い。
    `<3>1.` この段は、まだ初期化されていないグローバル `g` の初期化子の活性化 `b` を作る。`Obl(b)` は
      空で始まる。
      BY D24 の (E7), D1 (`init` はパラメータも capture も持たない)
    `<3>2.` `b` が終わって参照が `E` に入るのは (E4) の段であり、その後の印付けは (E5) の段である。
      BY D24 の (E7) (「`b` が終わると、(E4) と同じく ... 続けて (E5) の段が走り、`a` が再開する」)
    `<3>3.` `a` が `g` を読む段で参照を得ることは無い。
      BY D24 の (E7) の最後の段落, A8, D26
      CODE `src/generator.rs: Generator::add_global_object` (「A boxed global is moved out when read,
      so it needs no retain」 -- `retain_on_read` は boxed のとき偽)
    `<3>4.` QED
      BY `<3>1`, `<3>2`, `<3>3`

  `<2>9.` (F) の解放は、それが起きる段の中の素動作である。
    `<3>1.` `o` が `Destructor` のオブジェクトでないとき、(F) は (ε) 1 つである。`o` が持つ参照
      (D25) をすべて処分し、それから `o` の記憶域を返す。
      BY D24 の (F) の第 1 段落
      CODE `src/generator.rs: Generator::build_release_boxed_with` (`destruction_bb` が
      `traverse_refs` を走らせてから `build_free_boxed` を呼ぶ)
      CODE `src/object.rs: build_free_boxed`
    `<3>2.` `o` が `Destructor` のオブジェクトであるとき、(F) は次の列である。
      (a) `o` の `_value` の leaf の参照についての (α) (`o` から `Obl(b)` へ)。
      (b) `o` の `_dtor` の leaf が指すオブジェクトへの (β) (持ち手は `Obl(b)`)。
      (c) `b` と、`b` が返す `IO` の動作の活性化が行う素動作の列。
      (d) その活性化が返す参照についての (α) (`o` の `_value` の leaf へ)。
      (e) (ε) 1 つ。
      BY D24 の (F) の `Destructor` の段落, (H-F)
      CODE `src/generator.rs: Generator::build_run_destructor` (`_value` と `_dtor` を
      `move_out_struct_field` で取り出し、`build_retain(dtor, one, ...)` を置いてから
      `apply_lambda(dtor, vec![value], false)` を呼び、`run_io_or_ios_runner` の結果を
      `move_into_struct_field` で `_value` へ戻す)
      CODE `src/object.rs: ObjectFieldType::move_out_struct_field` (`extract_field_object` を呼ぶ
      だけで参照カウントに触れない)
      CODE `src/object.rs: ObjectFieldType::move_into_struct_field` (「taking over the caller's
      reference to `field`」)
      CODE `src/generator.rs: Generator::apply_lambda` (引数と capture ポインタをそのまま渡し、
      参照カウントに触れない)
    `<3>3.` (c) の活性化の中では、`<2>2` から `<2>9` の場合分けが同じように当たる。その内側に D24 の
      時点は無いが、`<1>4` は素動作について述べる主張なので、時点の有無を読まない。
      BY D24 の (F) (「(F) が作る活性化はその段の中で終わるので、その内側に D24 の時点は無い」),
      `<1>4`
    `<3>4.` `H(o)` が 0 になったオブジェクトの連鎖は有限で終わり、段の終わりには `H = 0` の計数下の
      オブジェクトはすべて解放されている。
      BY D24 の (F) (「この連鎖はオブジェクトの個数で抑えられるので有限で終わり」)
    `<3>5.` QED
      BY `<3>1`, `<3>2`, `<3>3`, `<3>4`

  `<2>10.` QED
    D24 は段を (E1) から (E7) の 7 種と定め、(F) は段ではなく、参照を処分するどの段の中でも起きる動作
    であるとする。(E2) は `<2>2` と `<2>3` で尽きる -- op が活性化を作るかどうかで分ける。(E6) の
    「プロセスを終える op」は `<2>2` の `<3>13` の場合分けから外してある。
    BY D24, `<2>1` - `<2>9`

`<1>6.` QED

  `<2>1.` `INV(t_0)` が成り立つ。
    BY `<1>3`
  `<2>2.` 各 `i ≥ 1` について、`INV(t_{i-1})` ならば `INV(t_i)` である。
    `<3>1.` 段 `s_i` は素動作の有限列である。
      BY `<1>5`
    `<3>2.` 各素動作は `H = A + B + C` を保つ。
      BY `<1>4`
    `<3>3.` QED
      BY `<3>1`, `<3>2` (有限列に沿って `<3>2` を繰り返す)
  `<2>3.` すべての時点で `INV` が成り立つ。
    BY `<2>1`, `<2>2`, `<1>2` (時点は `t_0, t_1, ...` で尽きる) と自然数の帰納法
  `<2>4.` QED
    BY `<2>3`, `<1>1`

## 3. この証明が使わなかったもの

**(S-c) は使わない。**解放されたオブジェクトのカウントに触れる本体でも、`H = A + B + C` は崩れない。
`Retain` は (β) であり、`Release` は (δ) であり、2 度目の (ε) は D25 が `o` に参照を帰属させないので
何も処分しない。P28 が要るのは (S-a) と (S-b) の 2 つだけである。

**A18 は使わない。**(a) の非巡回性は (F) の連鎖の停止に要らない -- D24 が停止をオブジェクトの個数で
述べている。(b) のグローバル状態のオブジェクトが計数下を指さないことも要らない -- `<1>4` の `<2>6` は
`B` の定義が計数下かどうかを問わないことだけを使う。

**2 つの実行の対応 (D30) も、観測 (D18、D19) も現れない。**P28 は 1 つの実行についての主張である。

## 4. README について

### 4.1 (H-D12) -- P28 には仮定が要る

README の P28 の言明は「実行 (D24) の各時点において、処分されていない各参照は、D25 が挙げる 3 種の
持ち手のちょうど 1 つに属する」であり、`P` についての仮定を持たない。この形は偽である。

**(S-a) が無いと偽である。**所有するパラメータ `y` を 1 つ持ち、本体が `Release(y, [], s, Release(y, [],
s, Ret(u)))` である関数を考える。初期の `Obl` は `y` の leaf の参照 1 つであり、1 つ目の `Release` が
それを取り除いて `H` を 1 下げる。2 つ目の `Release` は D10 に従ってもう一度 `H` を下げるが、取り除く
参照は `Obl` に無い。`H` が 1 減って `A + B + C` が減らないので、その時点で `A + B + C > H` となる。
すなわち 1 つの参照を 2 つの持ち手が持っている。

**(S-b) が無いと偽である。**所有するパラメータ `y` を 1 つ持ち、本体が `Ret(u)` (`u` は `y` を含まない)
である関数を考える。終端の `Ret` の消費の後も `Obl` に `y` の参照が残り、その活性化は終わる。以後その
参照はどの持ち手にも属さないので `A + B + C < H` となる。

**要る形**: P28 の言明に「`P` が D12 を満たすとき」を足す。読む者にとって足枷にはならない -- README の
第 7 節が挙げる P28 の読み手は A19 (i) であり、A19 の量化の範囲は「`borrow_ify` の入力の各本体と、
`borrow_ify` がそれを写した各本体」であって、そのどちらも A1 と P14 が D12 を与える。

### 4.2 (H-L) -- 「生きているオブジェクト」に定義が無い

D25 は「**生きているオブジェクト** `o'`。A5 より、`o'` が保持する値の boxed leaf のうち、inhabited で
あって計数下のオブジェクト (D26) を指すものが 1 つずつ持つ」と書く。この語の定義は D7 にも D24 にも
無い。D7 は「`H(o)` が 0 になったオブジェクトは**解放される**」とだけ言い、A18 (a) は「生きている
オブジェクトのグラフは非巡回である」と、D32 は解放の走査について、いずれもこの語を使う側である。

**要る形**: 「割り当てられて、まだ解放されていないオブジェクト」。

### 4.3 (H-A) -- (E2) の行き先の欄が 2 つ足りない

D24 の (E2) の行き先の表は `Llvm` の行の行き先を

> その op が書き込む先 (この段が割り当てるオブジェクト、または環境)、あるいは処分

と書く。**この欄に無い行き先が 2 つある。**

**(1) この段が作る活性化の `Obl`。** 同じ (E2) の後段は「その op の生成コードがオペランドを関数として
適用するとき (`LLVMGen::applies_a_function_operand` が真を宣言する op)、適用された関数の本体の活性化が
作られ ... (E3) と同じ形であり、違うのは呼び出し先を決めるのが `callee` の値ではなく op の生成コードだと
いうことだけである。`Obl(a)` の動きは D9 の `Llvm` の行と A3 の宣言が決めるので、この表の行はそのまま
である」と書く。この 2 文は行き先について食い違う。

- 「この表の行はそのままである」と読むと、適用された関数へ渡る参照の行き先が欄に無い。
  `Option::mod_some` の modifier がその参照である。この op は `applies_a_function_operand` を真と宣言し、
  `result_prov` を override しないので既定の `Unknown` を取る。よって modifier の boxed leaf (クロージャの
  capture) は D9 の `Llvm` の行が消費し、生成コードはそれを `apply_lambda(modifier, vec![value], false)`
  へ渡す。行き先は作られた活性化の `Obl` である
  (`CODE src/fixstd/builtin.rs: InlineLLVMUnionModBody`,
  `CODE src/ast/inline_llvm.rs: LLVMGen::result_prov` の既定)。
- 「(E3) と同じ形」と読むと、D9 の `Llvm` の行が消費する参照が**すべて** `Obl(b)` の初期値になる。
  同じ op がこれを破る -- union のオペランド `obj` も同じ行が消費するが、適用に渡るのは payload であって
  union ではなく、タグが合わない腕では modifier が `gc.release(modifier, RcState::Unknown)` で処分される
  (`CODE src/fixstd/builtin.rs: InlineLLVMUnionModBody`)。

**(2) 既に在るオブジェクトの leaf。** 一意性検査で分岐する op は、一意の腕でオペランドのオブジェクトを
そのまま使う (A3)。`struct_set` の一意の腕は、消費したオペランド `value` の参照を、この段が割り当てた
のではないオブジェクトへ書き込む
(`CODE src/fixstd/builtin.rs: InlineLLVMStructSetBody` -- `force_unique_or_assert` の後に
`ObjectFieldType::move_into_struct_field(gc, struct_obj, self.field_idx, &value)`)。欄の括弧書きは
「この段が割り当てるオブジェクト」しか挙げていない。

**要る形**: 第 0 節の (H-A) の 4 つ。

### 4.4 (H-F) -- (F) が `Destructor` の活性化の初期 `Obl` の出どころを述べていない

D24 の (F) は「解放とは、`o` が持つ参照 (D25) をすべて処分し、それから `o` の記憶域を返すことである」と
書き、続けて「解放される `o` が `Std::FFI::Destructor` のオブジェクトであるとき、解放は活性化を作る。
`o` の参照を処分する**前に**、`o` が `_dtor` の欄に持つ関数が `_value` の欄の値に適用され、返った `IO` の
動作が走る」と書く。

**この 2 文をそのまま足すと、1 つの参照を 2 回処分することになる。**適用は D9 の `App` の行の消費を行い、
その消費が `_value` の参照を渡す。ところが「`o` が持つ参照をすべて処分し」を解放の始めの `o` の値で読むと、
同じ `_value` の参照をもう一度処分することになる。`H` が 2 減って持ち手の側は 1 しか減らないので、
`A + B + C > H` になる。

コードはそうなっていない (`CODE src/generator.rs: Generator::build_run_destructor`)。

- `move_out_struct_field` は欄を読むだけで参照カウントに触れない。`_value` の参照は `apply_lambda` が
  消費する。
- `_dtor` については `build_retain(dtor.clone(), one, RcState::Unknown)` が参照を 1 つ新しく作り、
  `apply_lambda` がそれを消費する。欄が持つもとの参照は残る。
- 走った結果は `move_into_struct_field` で `_value` の欄へ戻る。
- その後に走る `traverse_refs` が処分するのは、`_value` に入った結果と、もとの `_dtor` である
  (`CODE src/generator.rs: Generator::build_traverser_work_nonnull_boxed_with` --
  `build_run_destructor` を `traverse_refs` の**前**に置く)。

**要る形**: 第 0 節の (H-F) の 4 つ。

### 4.5 実行の最初の状態が定まっていない

D24 は「プログラム `P` の**実行**とは、**段**の有限または無限の列である」とだけ書き、第 1 段の前の状態に
ついて何も述べない。`<1>3` はそこを D8 の「参照は D10 の**生成**によって作られ」を作り手の全列挙として
読んで埋めている。

この読みには帰結が 1 つある。**第 1 段が (E1) であるとき、その活性化の初期 `Obl` は空でなければならない。**
A17 (i) は「環境が活性化を作るとき、D10 の初期値が要求する参照を渡し」と書くが、渡す参照はまだ 1 つも
存在しないからである。C のエントリ点はこれを満たす -- 読むグローバル `main` の値が到達するオブジェクトは
(E5) を経てグローバル状態であり、渡す `IOState` は `is_unbox` の primitive で boxed leaf を持たない
(`CODE src/build/build_object_files.rs: build_main_function`,
`CODE src/fixstd/builtin.rs: run_ios_runner`, `CODE src/fixstd/builtin.rs: bulitin_tycons` --
`IOState` の `TyConInfo` は `is_unbox: true`, `fields: vec![]`)。`FFI_EXPORT` のエントリ点が boxed な
引数を取るときは、その参照は C の呼び出し元が持っている。実行の中でそれを C の側へ渡した段があれば
(E4) か (E2) の環境行き先がそれを説明するが、実行の外から来る場合は説明が無い
(`CODE src/ast/export_statement.rs: ExportStatement::implement` -- `Object::from_parts` が C の
引数から Fix の値を作る)。

### 4.6 (E7) が環境からのグローバル読みを覆っていない

D24 の (E7) は「**生きている活性化** `a` が、まだ初期化されていないグローバル `g` を読む節点の位置に
あるとき」と書く。環境がグローバルを読む場合が抜けている。C のエントリ点は、活性化が 1 つも無い時点で
グローバル `main` を読む (`CODE src/build/build_object_files.rs: build_main_function` --
`gc.get_scoped_obj(&main_name)`)。`FFI_EXPORT` のエントリ点も、書き出された値のグローバルを同じように
読む (`CODE src/ast/export_statement.rs: ExportStatement::implement` --
`gc.get_scoped_obj(&fix_name)`)。どちらもアクセサを通るので、初期化されていなければ初期化子の活性化が
作られる (`CODE src/rc_ir/codegen.rs: Generator::implement_rc_global`)。

P28 はこの穴に触れない -- `Obl(b)` は空で始まり、`b` が返す参照は `E` に入って (E5) でグローバル状態に
なる。すなわち `<2>8` の議論は読み手が環境でも活性化でも同じである。

### 4.7 (E2) の `H` の表の `Unknown` の行が 1 つの op に当たらない

D24 の (E2) の `H` の表は、`result_prov` が単一の `Unknown` を宣言する結果の leaf を「既存の
オブジェクトで +1」とする。`InlineLLVMBoxedFromRetainedPtrIOS` はこの行に当たらない。この op は
`result_prov` を override せず既定の `Unknown` を取る (A3 がそう述べる) が、生成コードは retain を
1 つも出さない -- オペランドの `Ptr` から番地を取り出して結果の欄へ入れるだけである
(`CODE src/fixstd/builtin.rs: InlineLLVMBoxedFromRetainedPtrIOS`)。その参照は C の側 (環境) が持って
いたものであり、この段で `E` から `Obl(a)` へ渡る。D24 の (E2) の 2 つの表は、`Obl(a)` を**離れる**参照と
**作られる**参照しか挙げず、`E` から `Obl(a)` へ**入る**参照を挙げていない。

P28 はこの行を字義どおりに読んでも閉じる -- (β) として `H` が 1 上がり、`E` は自分の参照を持ち続ける
ので、`H = A + B + C` は保たれる。食い違うのはモデルとコードの間であって、モデルの中ではない。

### 4.8 (E5) の位置が D22 と食い違う

D24 の (E5) は「グローバル初期化子の活性化が終わり、その参照が `E` に入った後 (E4)、環境が `mark_global`
で ... 印を付ける」と書く。D22 は「`mark_global` で印を付けるのは `InitValue#<symbol>` の側であり、
本体を評価した直後、値を返す前に行う」と書く。コードは D22 の側である
(`CODE src/rc_ir/codegen.rs: Generator::implement_rc_global` -- `eval_rc_expr` の直後に
`self.mark_global(obj.clone())` を置き、その後に `build_return`)。

P28 はどちらの順でも閉じる。`<2>6` が使うのは (ζ) が参照を作らず渡さず処分しないことだけであり、
消える参照を持っていたのが `E` でも `Obl(b)` でも議論は同じである。
