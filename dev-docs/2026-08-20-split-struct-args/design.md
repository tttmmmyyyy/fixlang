# 構造体の引数をフィールドごとの引数に割り、作った構造体をその場で読む

## 直すもの

`Iterator` の組み合わせ子は、渡された関数を自分の構造体のフィールドに置く。

```fix
type MapIterator i a b = unbox struct { iter : i, f : a -> b };
map = |f, iter| MapIterator { iter : iter, f : f };
```

`closure_specialization` が関数の値を追う道は「引数として渡された関数」と「持ち上げられたラムダの
キャプチャリストのフィールドに入った関数」の 2 つで、構造体のフィールドはどちらでもない。だから
`f` はクロージャの値のまま残り、`fold` は要素ごとに間接呼び出しをする (#450)。組み合わせ子を
重ねると、その間接呼び出しが段の数だけ並ぶ (#452)。

## 採る形

パスを 2 つ置く。1 つ目が `f` を引数の位置へ出し、2 つ目が引数へ渡る道を通す。

### 1. 構造体の引数をフィールドごとの引数に割る (`split_struct_args`)

グローバル関数 `f` の第 i 引数が非ボックス構造体 `T { g0 : A0, .., gn : An }` で、本体がそれを
分解しているとき、2 つに割る。

```
f#split_arg<i> : .. -> A0 -> .. -> An -> ..;
f#split_arg<i> = |p0, .., x0, .., xn, ..| <f の本体から分解を除いたもの>;

f = |p0, .., pi, ..| (
    let T { g0 : x0, .., gn : xn } = pi;
    f#split_arg<i>(p0, .., x0, .., xn, ..)
);
```

`f` は型も意味も変わらないので、**呼び出し側は 1 か所も書き換えない**。1 周につきグローバル 1 つに
つき引数 1 つを割り、プログラムが動かなくなるまで繰り返す。フィールドが構造体ならそれは twin の
引数になり、次の周がそれを割る。だから
`FilterIterator { MapIterator { ArrayIterator { arr, idx }, f }, pred }` は
`(arr, idx, f, pred)` へ 1 段ずつ平らになる。

割った後に残る wrapper は「構造体を分解してフィールドを渡す」形で、これは割る対象の形そのものである。
だから wrapper は割らない。割るべきものは twin の側に在る。

twin の中で元の名前を呼んでいる所は、twin を呼ぶように書き換える。twin が持つのは元の本体なので、
自分を呼ぶ所は元の名前 — つまり割った後の wrapper — を名指している。そのままだと自己再帰の関数が
**相互再帰の 2 つ**になり、`inline` は相互再帰を「自分を呼ぶ本体」と見なさないので輪をほどいて
両方を大きくする。twin を名指せば自己再帰は自己再帰のままで、wrapper は呼び出し側へ入る大きさに留まる。

twin にも wrapper にも局所名を付け直す。フィールドはパターンが与えた名前を持ち、構造体に入れ子に
なった構造体は同じ名前でフィールドを取り出すパターンで分解されるので、2 周割ると 1 つの名前の
引数が 2 つ並ぶ。

### 2. 作った構造体を、それを分解する側で読む (`collapse_constructions`)

`inline` が wrapper を呼び出し側へ入れると、そこには「構造体を作る式」と「それを分解する
パターン」が並ぶ。作ってすぐ分解する値は、実行時に、プログラムがすでに言っていることを訊いている。
そこで次の 4 つを不動点まで回す。

- **構造体を作った所で読む。** `let T { g : a } = s` で `s` が `T { g : x }` として作られていれば、
  `a` は `x` である。
- **union を作った所で読む。** 作られた variant が分かっている値への `match` は、その variant が
  選ぶ腕そのものである。
- **場合分けの中へ `match` を入れる。** `match` の走査対象が場合分け (`match` または `if`) で、その
  腕がすべて variant を作るなら、読む側の `match` を各腕へ移す。移した先では各腕が「作った所」に
  なるので、上の規則が消す。腕どうしが読む側の相異なる腕を選ぶ形でだけ移す。そうすれば腕の本体は
  1 つも複製されず、1 周ごとにプログラムは小さくなる。
- **フィールドの式を名前に落とす。** 構造体のフィールドが式なら、先に名前へ束縛する。読む側へ渡せる
  のは名前であって、式を渡すとその式が 2 度評価される。

構造体とそれを読む側は 1 本の `let` の鎖に並んで初めて出会うので、周と周の間に `pull_let` で `let`
を外へ巻き上げ、`unique_local_names` で局所名を一意にする。

3 つ目の規則が要るのは、`Iterator` の `advance` が `Option (Self, Item)` を返すからである。組み合わせ子の
`advance` は、内側の `advance` の結果を `match` して、腕ごとに `Option` を作り直す。`fold` はそれを
`match` する。この往復を越えないと、再帰呼び出しで関数の同一性が失われ、1 要素目だけが特殊化される。

## なぜこの形か

**正しさが構成で決まる。** `split_struct_args` の wrapper は元の signature と元の意味を持ち、twin は
元の本体そのものである。`collapse_constructions` の 4 つはどれも、その場に見えているものだけを見て
書き換える。どれも「この本体に在るこの型の値はすべて〜」という全称の主張を必要としない。型検査が
呼び出し側の更新を強制するので、割り忘れはコンパイルエラーになる。

**組み合わせ子の重ね (#452) も解ける。** 割るのを繰り返せば、内側の `f` も外側の `pred` も引数に
なる。フィールドの道を 1 段から n 段へ広げる必要が無い。

## パス列のどこに置くか

```
.. -> defunctionalize_fix -> inline -> inline_local
    -> split_struct_args -> inline -> collapse_constructions -> closure_specialization -> ..
```

- `inline_local` の後にするのは、`fold` の本体が構造体の引数を分解するのは `advance` がそこへ
  入った後だからである。その前に置くと `advance` だけが割れる。
- `collapse_constructions` の前に `inline` を置くのは、wrapper が呼び出し側へ入って初めて構造体と
  それを読む側が出会うからである。
- `closure_specialization` の前に置くのは、そこが読む名前を作るのがこの 2 つだからである。

どちらも `Max` 以上で走る。

## 割る条件

正しさには要らない。引数が無闇に増えるのと、割っても損になる形を避けるための条件である。

- 引数の型が、プログラムが宣言した**非ボックス**の構造体で、穴の開いたフィールドを持たない。
- フィールドが 1 つ以上ある。
- 本体がその引数を分解している。分解しないなら、割っても中身は使われない。
- 割った後の引数の総数が 24 を超えない。
- **戻り値の型がその構造体の型構成子を含まない。** 渡された種類の値をそのまま返す関数は、呼び出し側で
  必ず組み立て直される。分解を呼び出し側へ移すだけになる。

最後の条件が効くのは `FilterIterator` の `advance` である。これは自分自身を再帰的に呼ぶので `inline`
が `fold` へ入れられず、グローバルとして残る。これを割ると `fold` が要素ごとに 3 段の構造体を分解し、
`advance` が同じものを組み立て直して返す。この条件を外すと `map+filter+fold` が 29.02 から 35.01、
`map+filter+to_array` が 33.04 から 40.54 になる。

## 測ったもの

1 要素あたりの命令数。`(I(11) - I(1)) / 10 / 300000` を `perf stat -e instructions:u` で取る。

| chain | 前 | 後 |
|---|---|---|
| `map` | 14.00 | **0.84** |
| `map` + `map` | 22.00 | **0.97** |
| `filter` | 20.02 | **12.02** |
| `map` + `filter` + `fold` | 29.02 | **21.02** |
| `map` + `filter` + `to_array` | 33.04 | **25.04** |

`filter` を含む形が動くのは、`FilterIterator` の `advance` から飛ばすループを外へ出したからである
(下の「`filter` の自己再帰」を見よ)。

コーパス 51 ケースの命令数 (`-O experimental`、`perf stat -e instructions:u`、比較の相手はこのブランチが
含む `main` のマージベース。出力は 51/51 一致し、終了状態はすべて 0):

| case | 前 | 後 | |
|---|---|---|---|
| `bounds_check_indexable` | 69,286,596 | 289,679 | **-99.58%** |
| `sum_by_fix` | 143,109 | 139,757 | -2.34% |
| `cp_lib_segtree` | 75,215,869 | 74,915,876 | -0.40% |
| `cp_lib_bipartite` | 78,069,621 | 77,984,010 | -0.11% |
| `cp_lib_lsegtree` | 850,497,131 | 849,797,287 | -0.08% |
| `cp_lib_scc` | 149,194,965 | 149,294,968 | +0.07% |
| `cp_lib_unionfind` | 128,508,058 | 128,756,900 | +0.19% |
| 残り 44 ケース | | | 0.00% |

LangArena の 50 本はチェックサムが 50/50 通り、13 本を計った命令数は改善 3・中立 7・退行 3 で、
退行の最大は `CSV::Parse` の +1.35% である。`Json::ParseDom` の +0.53% はアロケータの当たり外れで、
12 億回の確保に対して回数の差は 43 回、解放は完全に一致し、ホットな関数はどれも構造的に同一である。

## 低い最適化水準での費用

`filter` / `filter_map` / `flatten` から飛ばす仕事を外へ出すのは、それを回収するパスが走る水準でだけ
得になる。`Max` 未満では、外へ出した本体への呼び出しと、それが返す `Option` を `advance` が包み直す
往復が、そのまま費用として残る。`-g` は水準を `None` にするので、デバッグ用のビルドもこれを払う。

1 要素あたりの命令数 (300,000 要素、マージベースとの比較):

| 連鎖 | `-O none` | `-O basic` | `-O max` |
|---|---|---|---|
| `filter` | 2391.98 -> 3075.72 (+28.6%) | 274.52 -> 441.45 (+60.8%) | 20.02 -> **12.02** |
| `filter_map` | 2538.42 -> 2808.24 (+10.6%) | 284.03 -> 303.11 (+6.7%) | 20.52 -> **12.52** |
| `flatten` | 2670.43 -> 3201.85 (+19.9%) | 412.36 -> 474.36 (+15.0%) | 24.11 -> **17.13** |

**この往復こそが `-O max` の利得の源である。** `_skip_until` が `FilterIterator` を自分で組み立てて
`advance` を一行にすると、低い水準の費用は完全に消える (`-O basic` でマージベースと同値の 274.52) が、
`-O max` の利得も消えて 20.02 に戻る。戻り値が `FilterIterator` を持ち帰ると `split_struct_args` が
割るのを断るからで、「ループを反復子から切り離すこと」と「往復を作ること」は同じ一手である。

`-O none` は元からこの 120 倍の水準にある (2392 対 20) ので、そこでの 29% は絶対量として小さい。

## コンパイル時間

分割されたグローバルの**呼び出し箇所が多い**プログラムでは、`uncurry` が受け取る `let` の鎖が倍近くに
なり、そこの既存の超線形な費用が増幅される。2 フィールドの構造体を分解する自己再帰のグローバルを F 個
作り、`main` からその全部を呼ぶ合成入力で、cold な `-O max` ビルドの命令数はマージベース比で
F=100 のとき 1.28 倍、F=200 で 1.54 倍、F=400 で 1.85 倍になる。費用は `uncurry::run` に落ちる
(F=400 で 12.0 秒 対 3.6 秒)。新しいパス自体は安い (0.07 秒と 0.44 秒)。

同型でも呼び出し箇所を増やさない入力では +3.0%、実在の `cp_lib_lsegtree` では +0.9%、48 段の `map`
連鎖ではこちらの方が速い。軸は呼び出し箇所の数である。

## `filter` の自己再帰

`FilterIterator` の `advance` は、述語が落とした要素を越えるために自分を呼んでいた。自分を呼ぶ本体は
`inline` が動かさないので、消費するループはそれを受け取れない (#453)。飛ばす仕事を別の本体
(`_skip_until`) へ出すと `advance` は消費側へ入る大きさになり、述語は要素ごとに反復子から読まれるのでなく
引数として渡る。

**この 2 つは組んで初めて効く。** ライブラリの変更だけでは `filter` が 20.02 から 24.53 へ遅くなる。述語が
`FilterIterator` のフィールドから読まれる限り同一性が追えず、それは #450 の壁そのものだからである。
引数を割って初めて述語が既知になり、`_skip_until` が特殊化される。

`_skip_until` の中身は `Std::loop` でも直接の末尾再帰でも命令数が同値である (12.02 / 21.02 / 25.04)。

### baseline の二値を検証せずに使うと### baseline の二値を検証せずに使うと

このコーパスは 5 度測った。1 度目と 2 度目の比較相手は「パスのゲートを `false` に書き換えてビルドした
二値」で、3 度目にそれが**ゲートの効いていない二値**だったことが分かった (`q_map1` が 14.00 でなく 0.84 を
返した)。4 度目は `main` をマージした後も比較相手を fork point のままにしていたので、`main` の 86 commit が
差に混ざった (`nbody` が +27% と読めた)。

比較相手は**そのブランチが含む `main` のマージベース**を、そのままチェックアウトしてビルドしたものを使う。
そして**使う前に、答えの分かっているケースで検証する**。

### 相互再帰を残したときに起きること

twin の再帰呼び出しを wrapper に向けたままにすると、`inline` が 2 つを互いへ入れて輪をほどく。
`cp_lib_conv_zp` では両方が大きくなって呼び出し側へ入らなくなり、`fold` が特殊化されず、
`convolve_zp` と `fft` が locality のクローンを失って FFT の内側で malloc と free が動いた
(+17.95%)。引数の provenance が `[fresh | unknown]` になるのはその結果であって、原因ではない。

### 読み手を失った構築を落とす

畳んだ後に構築が残ると、それが持つ参照が読み手の持つ参照と 2 つ並ぶ。このパスで落とすようにしていたが、
RC IR の `dead_code_elim` が同じものを落とすので外した。外して測ると、連鎖の 1 要素あたりの命令数は
6 本とも 1 つも動かない。

### 周と周の間に `inline` を走らせる

wrapper が呼び出し側へ入れば呼び出し側にも分解が現れるので、そこを次の周で割れる。そうすると
`filter` 単体が 15.00 から 20.02 へ戻り、`map+filter` の退行は消えなかった。
